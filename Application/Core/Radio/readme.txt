These files are required by the Middlewares\Third_Party\SubGHz_Phy subsystem, which
is used unmodified.  That subsystem assumes that these header files are on the
search path and that this c file includes essential functions for that middleware.
